import {ReactComponent as HappySvg} from '../../Assets/timeLineEmojies/happy.svg' ; 
export default function HappySvgComponent({classes}) {
     return(
          <HappySvg className={classes} />
     )
}